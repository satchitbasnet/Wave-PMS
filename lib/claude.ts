// Anthropic Claude API wrapper — Phase 6
// Set ANTHROPIC_API_KEY in .env.local when enabling AI features.

export async function askClaude(_prompt: string): Promise<string> {
  throw new Error("Claude integration is not configured yet. Add ANTHROPIC_API_KEY.");
}
